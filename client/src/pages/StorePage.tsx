import { PagePlaceholder } from '../components/PagePlaceholder'

export function StorePage() {
  return (
    <PagePlaceholder
      title="Store"
      summary="Spend Coins on rewards. Sort, filter, search, and page through the catalogue."
      tasks="[51] and [52]"
    />
  )
}
