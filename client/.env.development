# Loaded automatically by Vite in development. Not a secret — the local API's http profile.
# Production has no default: see src/api/config.ts, which throws rather than let requests
# resolve against the frontend's own origin and return index.html with a 200.
VITE_API_BASE_URL=http://localhost:5193
