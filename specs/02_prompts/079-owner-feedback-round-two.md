## Task

[79] Owner feedback round, five items.
Five more from the next review. Currency figures lost their coloured chips, because a number in a
tinted pill inside a card that is also tinted is two backgrounds arguing. The household card gained
a member card per partner showing their totals, which is the one backend change in that whole
session: `HouseholdMemberResponse` gained `LifetimePoints`, `Coins` and `CurrentWinStreak`. A
deliberate disclosure, argued in the DTO's own remarks. The invite code hides itself once the
household is full, since there is nobody left to invite.

Its full specification is in `ui-exp02-design-review-rounds.md`, round D — that file
consolidates the consecutive design-review rounds, because they cut across many tasks at once
and reading them one at a time loses the argument that connects them.

Numbered file kept rather than deleted so the numbering has no silent gap.
