## Task

[62] Finalise the `/specs` folder so it covers the whole build rather than only the planning stage,
and decide what to do about the two parallel specs trees that had grown up.

**Done.** `01_architecture/` carries the design documents as built, alongside the earlier drafts,
which are kept because the planning steps are part of the record. `02_prompts/` carries the
specification written before each task, `000` through `092`, with no gaps in the numbering.
