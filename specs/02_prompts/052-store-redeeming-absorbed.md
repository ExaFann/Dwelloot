## Task

[52] *Absorbed into [51].* See `051-store.md`.

Was "redeeming" — the redeem button, the balance check and the confirmation state — split apart from
the Store catalogue. Folded into [51] on the same call that absorbed [49] and [50]: those controls
are *on that screen*, and shipping a catalogue you cannot buy from is the "looks finished, is not"
problem that got [46] and [47] sent back.

Rewards CRUD from [29] landed in [51] the same way chore CRUD landed in [47] — `POST`/`PATCH`/
`DELETE /api/rewards` have no other consumer, and without them `wireframes.md` §4's "defaults **+
custom**" is unreachable.

Numbered file kept rather than deleted so the numbering has no silent gap.
