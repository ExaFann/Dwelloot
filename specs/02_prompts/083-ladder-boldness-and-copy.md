## Task

[83] The period ladder, bolder type, kinder copy.
The owner's read was that the page whispered, so type and borders grew. The period panels became a
ladder rather than three equal columns. Copy across the app was rewritten away from a courtroom
register. The scrollbar experiment lives here too: `::-webkit-scrollbar` was restyled and the
platform ignored everything but the colour, which [85] then fixed properly by hiding the bar instead.

Its full specification is in `ui-exp02-design-review-rounds.md`, round D — that file
consolidates the consecutive design-review rounds, because they cut across many tasks at once
and reading them one at a time loses the argument that connects them.

Numbered file kept rather than deleted so the numbering has no silent gap.
