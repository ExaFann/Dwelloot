## Task

[81] Six animations, and marks for all three feed outcomes.
The six items the survey recommended: value pulses, row entry, the nav badge bump, a route fade, the
lead-change flip, and the badge celebration. All CSS keyframes, all one-shot, all with a
`prefers-reduced-motion` answer, and no animation library. The prize feed's three sources each got
their own outcome mark, so bought and won prizes stop looking identical.

Its full specification is in `ui-exp02-design-review-rounds.md`, round C — that file
consolidates the consecutive design-review rounds, because they cut across many tasks at once
and reading them one at a time loses the argument that connects them.

Numbered file kept rather than deleted so the numbering has no silent gap.
