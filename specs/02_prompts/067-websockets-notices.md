## Task

[67] The same hub extended to the Notices tab, so a new pending approval arrives without a refresh.

**Not built**, for the reasons in [66]. `liveSync.ts` covers this surface with the same polling
options, and a source-scanning test enforces that every query which the other person can change opts
into them.
